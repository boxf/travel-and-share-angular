import {TypeEnum} from './TypeEnum';

export class Place {
  id: number;
  name: string;
  grade: number;
  pictureName: string;
  userReview: string;
  xaxis: number;
  yaxis: number;
  typeEnum: TypeEnum;
  // type and county
}
